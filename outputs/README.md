# Outputs

This folder contains the packaged figures and tables selected for public GitHub upload.

Draft-style figures such as `*_corrected.png`, `*_fixed.png`, and test plots were intentionally left out of this cleaned package so the repository looks more professional.
